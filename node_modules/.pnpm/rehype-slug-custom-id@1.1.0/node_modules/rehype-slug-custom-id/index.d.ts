/**
 * Exported function to get a Node's ID
 *
 * @param {Node} node
 * @param {{
 *   enableCustomId?: boolean,
 *   maintainCase?: boolean,
 *   removeAccents?: boolean
 * }} props
 */
export function getHeaderNodeId(
  node: Node,
  props?: {
    enableCustomId?: boolean | undefined
    maintainCase?: boolean | undefined
    removeAccents?: boolean | undefined
  }
): {
  id: string
  isCustomId: boolean
}
/**
 * Plugin to add `id`s to headings.
 *
 * @type {import('unified').Plugin<[{
 *   enableCustomId?: boolean,
 *   maintainCase?: boolean,
 *   removeAccents?: boolean
 * }], Root>}
 */
export default function rehypeSlug(props?: {
  enableCustomId?: boolean | undefined
  maintainCase?: boolean | undefined
  removeAccents?: boolean | undefined
}):
  | void
  | import('unified').Transformer<import('hast').Root, import('hast').Root>
export const slugs: Slugger
export type Root = import('hast').Root
export type Node = import('hast').Node
import Slugger from 'github-slugger'
